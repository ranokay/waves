name: release-or-test-build

# Builds the Waves QML app for every platform and, on a version tag, publishes
# the binaries to a GitHub Release together with a signed checksum manifest the
# in-app updater verifies before installing.
#
# Publication model: releases are published from the upstream repository
# iamprivacy/Waves, which holds the signing key (`WAVES_SIGNING_KEY`). This fork
# is a development line. It holds no signing key, publishes no releases, and
# every build it produces checks upstream for updates. The fork's supported run
# is a rehearsal. Dispatch with a blank `release_tag`, or push `test-build`, to
# build the unsigned artifacts; the `vX.Y.Z` tag and `release_tag` paths belong
# to upstream. The comments below describe the machinery as it runs there.

permissions:
  contents: write

defaults:
  run:
    shell: bash

on:
  push:
    tags:
      - "v*.*.*"
    branches:
      - "test-build"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Publish a GitHub Release at this tag (e.g. v0.1.0). Upstream only; this fork cannot sign one, so leave it blank to rehearse. Blank = artifacts only."
        required: false
        default: ""
      only:
        description: "Comma-separated OS_ARCH legs to build (e.g. macos-intel,macos-apple-silicon,windows-x64; the legacy macOS legs are macos-intel_legacy / macos-apple-silicon_legacy, and naming one also runs its regular macOS leg because the filter is a substring match). Blank = all eight. Cannot be combined with release_tag: a release must carry every platform."
        required: false
        default: ""

# A tagged push builds the matrix and publishes a Release; a manual run with
# release_tag does the same under that tag, which is the supported fallback when
# a tag-push event does not reliably fire the build.
# The concurrency group makes a rerun cancel the prior long build instead of
# double-spending Actions minutes.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  ASSET_EXTENSION: ".zip"
  OUT_NAME_FILE: "waves"
  ARCH_MACOS_X64: "macos-intel"
  ARCH_MACOS_ARM64: "macos-apple-silicon"
  # Legacy macOS flavor (pyside6 6.9.3, floor 12.0) for macOS 12 through 14;
  # the regular macOS zips require 15 (the locked PySide6's real floor, see
  # pyproject.toml). Underscore before "legacy" on purpose: pre-dual-build
  # updaters in the field pick between assets by NAME SORT, and ".zip" sorts
  # before "_legacy.zip", so every old updater (all of which run on macOS 15+,
  # no earlier release could launch below that) keeps resolving to the regular
  # bundle. A hyphen would sort the legacy bundle first and downgrade them.
  ARCH_MACOS_X64_LEGACY: "macos-intel_legacy"
  ARCH_MACOS_ARM64_LEGACY: "macos-apple-silicon_legacy"
  ARCH_LINUX_X64: "linux-x64"
  ARCH_LINUX_ARM64: "linux-arm64"
  ARCH_WINDOWS_X64: "windows-x64"
  ARCH_WINDOWS_ARM64: "windows-arm64"

jobs:
  compute:
    # Workaround to be able to use env vars in the matrix.
    runs-on: ubuntu-24.04
    outputs:
      legs: ${{ steps.select.outputs.legs }}
      ASSET_EXTENSION: ${{ env.ASSET_EXTENSION }}
      OUT_NAME_FILE: ${{ env.OUT_NAME_FILE }}
      ARCH_MACOS_X64: ${{ env.ARCH_MACOS_X64 }}
      ARCH_MACOS_ARM64: ${{ env.ARCH_MACOS_ARM64 }}
      ARCH_MACOS_X64_LEGACY: ${{ env.ARCH_MACOS_X64_LEGACY }}
      ARCH_MACOS_ARM64_LEGACY: ${{ env.ARCH_MACOS_ARM64_LEGACY }}
      ARCH_LINUX_X64: ${{ env.ARCH_LINUX_X64 }}
      ARCH_LINUX_ARM64: ${{ env.ARCH_LINUX_ARM64 }}
      ARCH_WINDOWS_X64: ${{ env.ARCH_WINDOWS_X64 }}
      ARCH_WINDOWS_ARM64: ${{ env.ARCH_WINDOWS_ARM64 }}
    steps:
      - name: Compute outputs
        run: |
          echo "ASSET_EXTENSION=${{ env.ASSET_EXTENSION }}" >> $GITHUB_OUTPUT
          echo "OUT_NAME_FILE=${{ env.OUT_NAME_FILE }}" >> $GITHUB_OUTPUT
          echo "ARCH_MACOS_X64=${{ env.ARCH_MACOS_X64 }}" >> $GITHUB_OUTPUT
          echo "ARCH_MACOS_ARM64=${{ env.ARCH_MACOS_ARM64 }}" >> $GITHUB_OUTPUT
          echo "ARCH_MACOS_X64_LEGACY=${{ env.ARCH_MACOS_X64_LEGACY }}" >> $GITHUB_OUTPUT
          echo "ARCH_MACOS_ARM64_LEGACY=${{ env.ARCH_MACOS_ARM64_LEGACY }}" >> $GITHUB_OUTPUT
          echo "ARCH_LINUX_X64=${{ env.ARCH_LINUX_X64 }}" >> $GITHUB_OUTPUT
          echo "ARCH_LINUX_ARM64=${{ env.ARCH_LINUX_ARM64 }}" >> $GITHUB_OUTPUT
          echo "ARCH_WINDOWS_X64=${{ env.ARCH_WINDOWS_X64 }}" >> $GITHUB_OUTPUT
          echo "ARCH_WINDOWS_ARM64=${{ env.ARCH_WINDOWS_ARM64 }}" >> $GITHUB_OUTPUT
      - name: Check out (leg selection)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # The `only` filter selects matrix legs here, once, instead of
      # per-step guards in every leg: an excluded leg never becomes a job,
      # so no Success-with-skipped-steps conclusion can hide an unbuilt
      # platform. tools/select_build_legs.py keeps the documented substring
      # rule (naming a legacy leg also runs its regular twin); blank selects
      # all eight (releases and test-build pushes).
      - name: Select legs
        id: select
        env:
          ONLY_FILTER: ${{ github.event.inputs.only }}
        run: echo "legs=$(python3 tools/select_build_legs.py "$ONLY_FILTER")" >> "$GITHUB_OUTPUT"
      # A published Release ships binaries with the updater config baked in; if
      # REPO or the signing public key are blank, every copy is permanently unable
      # to auto-update and can't be fixed after download. Fail the whole run early
      # (before spending build minutes) so that can't happen by accident. Only
      # gates real releases; test-build / artifact-only runs are unaffected.
      - name: Check out (release-config guard)
        if: ${{ (github.event_name != 'workflow_dispatch' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '') }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Guard, a published release must be able to self-update
        if: ${{ (github.event_name != 'workflow_dispatch' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '') }}
        run: |
          grep -Eq '^REPO = "[^"]+"' waves/desktop/updater.py \
            || { echo "::error::updater.REPO is empty; a published build could never auto-update. Set REPO before tagging a release."; exit 1; }
          grep -Eq '^UPDATE_PUBLIC_KEY(: str)? = "[^"]+"' waves/desktop/signing.py \
            || { echo "::error::signing.UPDATE_PUBLIC_KEY is empty; shipped updates could not be signature-verified. Embed UPDATE_PUBLIC_KEY before tagging a release."; exit 1; }
          echo "Updater is configured; release build may proceed."
      # The version compiled INTO the binary must equal the release tag, or the
      # shipped build reports an old version while the signed manifest advertises
      # the new one; every updater then re-offers the "new" release forever
      # because the installed binary keeps reporting the old number. Guard both
      # the tag-push and dispatch+release_tag paths against a forgotten bump.
      - name: Guard, release tag matches __version__
        if: ${{ (github.event_name != 'workflow_dispatch' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '') }}
        env:
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
        run: |
          TAG_VERSION="${RELEASE_TAG#v}"
          CODE_VERSION="$(grep -m1 '__version__' waves/desktop/__init__.py | tr -d ' "' | cut -d'=' -f2)"
          if [ "$TAG_VERSION" != "$CODE_VERSION" ]; then
            echo "::error::release tag '$RELEASE_TAG' (version '$TAG_VERSION') does not match waves/desktop/__init__.py __version__ '$CODE_VERSION'. Bump __version__ to match the tag before releasing."
            exit 1
          fi
          echo "Version $CODE_VERSION matches release tag $RELEASE_TAG."
      # A dispatch naming a tag publishes a signed, live Release, and the `only`
      # filter is honored by every per-leg step including the asset uploads: the
      # two together published a release holding one platform's zip and a
      # manifest covering only it, so every field updater on the other platforms
      # saw a latest release with no asset for it and silently stopped offering
      # updates. `only` is for re-cutting a leg as an ARTIFACT run; refused here
      # rather than discovered once the Release is already public.
      - name: Guard, a release builds every platform
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '' && github.event.inputs.only != '' }}
        run: |
          echo "::error::release_tag and only cannot be combined: a release must carry every platform's asset. Leave only blank to publish, or drop release_tag to re-cut one leg as an artifact."
          exit 1
      # A manual release may be dispatched from any ref; only publish binaries
      # from a commit that is already on the release line. Otherwise a release
      # could ship from an unmerged feature branch, and (since target_commitish
      # points the created tag at the built commit) the public tag would name
      # code that was never reviewed onto the release line.
      - name: Guard, dispatch release commit is on the default branch
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '' }}
        env:
          # The repository's OWN default branch, never a hard-coded name. This
          # workflow lives in a private repo whose line is `master` and ships to
          # the public one whose only branch is `main`, and a dispatch release is
          # only ever meaningful on the public side: `git fetch origin master`
          # died there with "couldn't find remote ref", failing this job under
          # `bash -e` and skipping every build leg. That killed the documented
          # fallback (RELEASING.md) exactly when it is needed, which is when a
          # tag push has not fired the build.
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          git fetch --quiet origin "$DEFAULT_BRANCH"
          if ! git merge-base --is-ancestor "${{ github.sha }}" "origin/$DEFAULT_BRANCH"; then
            echo "::error::commit ${{ github.sha }} is not on $DEFAULT_BRANCH; refusing to publish a release from an unmerged ref. Merge into $DEFAULT_BRANCH first, then dispatch the release."
            exit 1
          fi
          echo "Dispatched commit is on $DEFAULT_BRANCH; release build may proceed."

  create-release:
    # Create the draft Release exactly once, before the build matrix starts.
    # Each platform leg must not independently ask softprops/action-gh-release
    # to "find or create" the Release by tag: running six of those at once is
    # a race, more than one leg can find nothing and create its own, splitting
    # the platform assets across duplicate drafts (observed on v0.1.2: two
    # drafts both tagged v0.1.2, assets scattered between them). Creating the
    # Release here, ahead of the matrix, removes the race: every leg's own
    # upload step (further below) then finds this one unambiguous Release by
    # tag instead of racing to create it.
    needs: ["compute"]
    if: ${{ (github.event_name != 'workflow_dispatch' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '') }}
    runs-on: ubuntu-24.04
    steps:
      - name: Create the Release (empty draft, assets attached by the matrix)
        uses: softprops/action-gh-release@e598afbe1493e6b1bafb1f389cabb956eab91231 # v3.0.3
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
          # Point a not-yet-existing tag at the exact commit that was built,
          # not the default branch tip. Ignored when the tag already exists (a
          # tag push), which is the common release path.
          target_commitish: ${{ github.sha }}
          # Keep the Release a DRAFT while binaries upload; the sign-manifest
          # job flips it live only after SHA256SUMS.sig is attached, so a
          # user's (fail-closed) updater never sees a published-but-unsigned
          # release.
          draft: true

  build:
    name: build ${{ matrix.leg.target }}
    needs: ["compute", "create-release"]
    # create-release is skipped outright on a non-release run (test-build /
    # a dispatch with no release_tag), which is fine; but if it's supposed to
    # run and fails, don't burn eight legs' worth of build minutes on a
    # release that has nowhere to land. (Hyphenated job id, so bracket syntax:
    # dot notation would parse "create-release" as a subtraction.)
    # Leg selection lives in the compute job's Select legs step (tools/select_build_legs.py):
    # the matrix below iterates only selected legs, so an excluded leg never
    # becomes a job at all — no Success-with-skipped-steps conclusions exist.
    if: always() && needs.compute.result == 'success' && needs['create-release'].result != 'failure' && needs['create-release'].result != 'cancelled' && (startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/test-build' || github.event_name == 'workflow_dispatch')
    strategy:
      fail-fast: false
      # The matrix iterates selected legs only (see the Select legs step):
      # excluded legs never start, so every job conclusion below means a
      # real build. Leg shapes live in build-legs.json next to this file:
      # macOS legs zip the trimmed Waves.app (the updater's exact shape);
      # both build on runners newer than WAVES_MACOS_MIN, so the floor is
      # asserted statically instead. Legacy legs overlay PySide6 6.9.3 with
      # WAVES_MACOS_MIN=12.0. Linux legs ship zip + AppImage. Windows legs
      # set --low-memory explicitly (MSVC C1002 on yt-dlp's generated C).
      matrix:
        leg: ${{ fromJSON(needs.compute.outputs.legs) }}
    runs-on: ${{ matrix.leg.os }}
    env:
      # ccache lives in the cached Nuitka cache root; its 5 GB default would
      # crowd GitHub's 10 GB repository budget across eight leg caches.
      CCACHE_MAXSIZE: "2G"
    steps:
      # Leg selection already happened in the matrix (see the Select legs
      # step): every job here builds, so steps carry no `only` guards.
      # (Releases and test-build pushes leave `only` blank, so every leg
      # still runs.)
      - name: Check out
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Set up the environment
        uses: ./.github/actions/setup-env
      # Carry Nuitka's build tree and its cache root (ccache, module cache,
      # downloads) between runs: content signatures skip recompiling modules
      # whose generated C is unchanged, so a warm cache turns a routine
      # release into a relink. Keyed per leg, on the lockfile and on the build
      # inputs that change the objects (the build script, mise.toml,
      # pyproject.toml and this workflow), so a flag or toolchain change never
      # reuses a tree built by
      # another configuration; the restore-key fallback is salted the same
      # way. The policy and the size budget live in docs/dependency-updates.md.
      - name: Restore the Nuitka build cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            dist/waves.build
            ~/.cache/Nuitka
            ~/Library/Caches/Nuitka
            ~/AppData/Local/Nuitka/Nuitka/Cache
          key: nuitka-v3-${{ matrix.leg.os_arch }}-${{ hashFiles('mise.toml', 'tools/build_waves.sh', 'pyproject.toml', '.github/workflows/release-or-test-build.yml', '.github/workflows/build-legs.json') }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            nuitka-v3-${{ matrix.leg.os_arch }}-${{ hashFiles('mise.toml', 'tools/build_waves.sh', 'pyproject.toml', '.github/workflows/release-or-test-build.yml', '.github/workflows/build-legs.json') }}-
      - name: Build Waves for ${{matrix.leg.target}}
        run: ${{matrix.leg.cmd_build}}
      # Smoke-launch the freshly built+trimmed bundle headlessly BEFORE it is
      # hashed and release-signed. trim_qt_bundle.sh strips aggressively; without
      # this, the first time a trimmed bundle ever runs is on a user's machine.
      # We launch offscreen with a short deadline and require a clean startup:
      # if the process dies before the deadline (a broken bundle / missing Qt
      # module at import) the job fails; a process still alive at the deadline is
      # a healthy launch and we terminate it. Runs on every build (release or
      # test) so a bad trim is caught before anything is published. Conservative
      # by design: no interaction, no display, no network.
      #
      # ARM runners can't natively execute a cross-arch build, so smoke-launch is
      # skipped there; native x64 legs cover the
      # trim on each OS.
      # The trimmed bundle links the system's GL stack (Nuitka does not bundle
      # it); desktop installs have it, but GitHub's headless runner images may
      # not (ubuntu-24.04 dropped libEGL). Install just enough to launch
      # offscreen; this mirrors a user's desktop, it is not shipped.
      - name: Install Linux GL runtime for smoke-launch
        if: ${{ runner.os == 'Linux' && !endsWith(matrix.leg.os_arch, '-arm64') }}
        run: sudo apt-get update -qq && sudo apt-get install -y -qq --no-install-recommends libegl1 libgl1 libopengl0 libxkbcommon0 libfontconfig1 libpulse0 libasound2t64
      - name: Smoke-launch trimmed bundle
        if: ${{ !endsWith(matrix.leg.os_arch, '-arm64') }}
        env:
          QT_QPA_PLATFORM: offscreen
          QT_OPENGL: software
        run: |
          set -u
          case "${{ matrix.leg.os_arch }}" in
            macos-*)   BIN="dist/waves.app/Contents/MacOS/Waves" ;;
            windows-*) BIN="dist/Waves/Waves.exe" ;;
            *)         BIN="dist/Waves/Waves" ;;
          esac
          if [ ! -e "$BIN" ]; then
            echo "::error::smoke-launch: built binary '$BIN' not found"; exit 1
          fi
          echo "Smoke-launching $BIN (offscreen)…"
          "$BIN" &
          APP_PID=$!
          ok=0
          # Poll for ~15s: success = still alive (a healthy long-running GUI);
          # failure = exited early with a non-zero status.
          for _ in $(seq 1 15); do
            sleep 1
            if ! kill -0 "$APP_PID" 2>/dev/null; then
              wait "$APP_PID"; rc=$?
              if [ "$rc" -eq 0 ]; then ok=1; else
                echo "::error::smoke-launch: $BIN exited early with status $rc (trimmed bundle fails to start)"; exit 1
              fi
              break
            fi
          done
          if kill -0 "$APP_PID" 2>/dev/null; then
            echo "smoke-launch: still running after startup window; healthy."
            kill "$APP_PID" 2>/dev/null || true
            wait "$APP_PID" 2>/dev/null || true
            ok=1
          fi
          [ "$ok" -eq 1 ] || { echo "::error::smoke-launch: $BIN never reached a healthy state"; exit 1; }
      # The smoke-launch above runs on the build host itself, so it can never
      # catch a bundle that demands a newer macOS than a user's machine has.
      # This step checks the version floor statically instead: every file in
      # the bundle that is a Mach-O (Qt libraries ship flattened WITHOUT an
      # extension, so scan everything) must not require anything newer than
      # the leg's MACOS_FLOOR (15.0 regular, 12.0 legacy; the same values the
      # legs feed WAVES_MACOS_MIN), and Info.plist must declare exactly that
      # value so an unsupported system shows a clear "requires macOS X" dialog
      # instead of a Dock bounce. A dependency bump that smuggles in
      # newer-floor binaries fails here instead of shipping a bundle whose
      # plist lies (PySide6 6.10 wheels are tagged macOS 13 but
      # contain macOS 15-only libraries).
      - name: Assert macOS version floor
        if: ${{ startsWith(matrix.leg.os_arch, 'macos-') }}
        run: |
          set -eu
          MIN="${{ matrix.leg.macos_floor }}"
          [ -n "$MIN" ] || { echo "::error::floor-assert: this macOS leg declares no MACOS_FLOOR in the matrix"; exit 1; }
          PLIST="dist/waves.app/Contents/Info.plist"
          DECLARED="$(plutil -extract LSMinimumSystemVersion raw "$PLIST" 2>/dev/null || true)"
          if [ "$DECLARED" != "$MIN" ]; then
            echo "::error::floor-assert: Info.plist LSMinimumSystemVersion is '${DECLARED:-missing}', expected '$MIN'"; exit 1
          fi
          min_of() {  # print the macOS floor a Mach-O file demands (empty if not Mach-O)
            # -arch all: a universal file carries one load-command set PER SLICE
            # and otool defaults to the host slice only; the floor is the most
            # demanding slice, so collect every version and keep the highest.
            otool -arch all -l "$1" 2>/dev/null | awk '
              /LC_BUILD_VERSION/ {b=1; m=0}
              /LC_VERSION_MIN_MACOSX/ {m=1; b=0}
              b && /minos/ {print $2; b=0}
              m && $1 == "version" {print $2; m=0}' | sort -V | tail -1
          }
          FAIL=0
          SCANNED=0
          while IFS= read -r F; do
            V="$(min_of "$F")"
            [ -n "$V" ] || continue
            SCANNED=$((SCANNED + 1))
            if [ "$(printf '%s\n%s\n' "$MIN" "$V" | sort -V | tail -1)" != "$MIN" ]; then
              echo "::error::floor-assert: $F demands macOS $V, above the declared floor $MIN"; FAIL=1
            fi
          done < <(find dist/waves.app -type f)
          [ "$SCANNED" -gt 0 ] || { echo "::error::floor-assert: no Mach-O files found under dist/waves.app"; exit 1; }
          [ "$FAIL" -eq 0 ] || exit 1
          echo "floor-assert: all $SCANNED Mach-O files honor the macOS $MIN floor."
      # The AppImage wraps AppRun + the runtime AROUND the already-smoked tree,
      # so launch the finished file itself too (x64 only, same reason as above;
      # --appimage-extract-and-run because runners have no FUSE).
      - name: Smoke-launch AppImage
        if: ${{ runner.os == 'Linux' && !endsWith(matrix.leg.os_arch, '-arm64') }}
        env:
          QT_QPA_PLATFORM: offscreen
          QT_OPENGL: software
        run: |
          set -u
          APPIMG="dist/${{needs.compute.outputs.OUT_NAME_FILE}}_${{matrix.leg.os_arch}}.AppImage"
          if [ ! -e "$APPIMG" ]; then
            echo "::error::smoke-launch: built AppImage '$APPIMG' not found"; exit 1
          fi
          echo "Smoke-launching $APPIMG (offscreen)…"
          "$APPIMG" --appimage-extract-and-run &
          APP_PID=$!
          ok=0
          for _ in $(seq 1 15); do
            sleep 1
            if ! kill -0 "$APP_PID" 2>/dev/null; then
              wait "$APP_PID"; rc=$?
              if [ "$rc" -eq 0 ]; then ok=1; else
                echo "::error::smoke-launch: $APPIMG exited early with status $rc"; exit 1
              fi
              break
            fi
          done
          if kill -0 "$APP_PID" 2>/dev/null; then
            echo "smoke-launch: AppImage still running after startup window; healthy."
            kill "$APP_PID" 2>/dev/null || true
            wait "$APP_PID" 2>/dev/null || true
            ok=1
          fi
          [ "$ok" -eq 1 ] || { echo "::error::smoke-launch: $APPIMG never reached a healthy state"; exit 1; }
      - name: Checksum
        # Portable per-asset SHA256 (no coreutils dependency on Windows). The
        # line is "<hex>  <filename>": coreutils sha256sum format, which the
        # signed SHA256SUMS manifest and the in-app updater both parse. Linux
        # legs carry a second asset (the AppImage); the loop hashes whichever
        # of the leg's assets exist.
        run: |
          cd dist
          BASE="${{needs.compute.outputs.OUT_NAME_FILE}}_${{matrix.leg.os_arch}}"
          for ASSET in "${BASE}${{needs.compute.outputs.ASSET_EXTENSION}}" "${BASE}.AppImage"; do
            [ -e "$ASSET" ] || continue
            python -c "import hashlib,sys; p=sys.argv[1]; open(p+'.sha256','w').write(hashlib.sha256(open(p,'rb').read()).hexdigest()+'  '+p+chr(10))" "$ASSET"
            cat "$ASSET.sha256"
          done
      - name: Upload checksum artifact
        # Tiny per-asset checksums collected by the sign job into one manifest
        # (the glob picks up the AppImage's sidecar on the linux legs).
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: sha256-${{matrix.leg.os_arch}}
          path: ./dist/${{needs.compute.outputs.OUT_NAME_FILE}}_${{matrix.leg.os_arch}}*.sha256
      - name: Upload Build Artifact (workflow_dispatch / test-build)
        if: ${{ github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/test-build' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{needs.compute.outputs.OUT_NAME_FILE}}_${{matrix.leg.os_arch}}
          path: |
            ./dist/${{needs.compute.outputs.OUT_NAME_FILE}}_${{matrix.leg.os_arch}}${{needs.compute.outputs.ASSET_EXTENSION}}
            ./dist/${{needs.compute.outputs.OUT_NAME_FILE}}_${{matrix.leg.os_arch}}.AppImage
      - name: Upload Release Asset
        # Publish only when a release is actually being made: a tag PUSH (not a
        # dispatch that merely happens to run on a tag ref), OR a dispatch that
        # supplies release_tag. A dispatch with a blank release_tag builds
        # artifacts only ("Blank = artifacts only").
        # The create-release job already made this Release (draft, empty)
        # before any matrix leg started, so every leg here finds the same
        # unambiguous Release by tag and just appends its two files, instead
        # of racing to create it themselves.
        if: ${{ (github.event_name != 'workflow_dispatch' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '') }}
        uses: softprops/action-gh-release@e598afbe1493e6b1bafb1f389cabb956eab91231 # v3.0.3
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          fail_on_unmatched_files: true
          tag_name: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
          target_commitish: ${{ github.sha }}
          draft: true
          # One glob per leg: zip + .sha256 everywhere, plus the AppImage and
          # its .sha256 on the linux legs. Each leg's OS_ARCH prefix is unique,
          # so a leg can only ever attach its own files.
          files: |
            ./dist/${{needs.compute.outputs.OUT_NAME_FILE}}_${{matrix.leg.os_arch}}*

  sign-manifest:
    # Runs for any release (a tag PUSH or a dispatch+release_tag): gather every
    # per-asset checksum into one SHA256SUMS manifest, sign it with the Ed25519
    # release key (CI secret WAVES_SIGNING_KEY, held by the upstream repository),
    # and publish SHA256SUMS + SHA256SUMS.sig, then flip the Release live. The
    # app verifies the signature against the public key baked into the binary
    # before trusting any hash, so a tampered release can't slip a malicious
    # build past the updater.
    needs: ["compute", "build"]
    if: ${{ (github.event_name != 'workflow_dispatch' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '') }}
    runs-on: ubuntu-24.04
    steps:
      - name: Check out
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Set up the environment
        uses: ./.github/actions/setup-env
      - name: Collect per-asset checksums
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: sha256-*
          merge-multiple: true
          path: sums
      - name: Build SHA256SUMS manifest
        # Bind the release version INTO the signed manifest (the `# waves-version`
        # line) so the updater's downgrade protection rests on authenticated data,
        # not the unsigned GitHub API tag. The '#' line is ignored by hash parsers
        # but is covered by the signature.
        env:
          # Use the dispatched release_tag when this is a manual release; on a
          # tag push github.ref_name IS the tag. Never the branch name.
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
        run: |
          { echo "# waves-version: ${RELEASE_TAG}"; cat sums/*.sha256 | LC_ALL=C sort -k2; } > SHA256SUMS
          echo "----- SHA256SUMS -----"; cat SHA256SUMS
      - name: Sign SHA256SUMS
        env:
          WAVES_SIGNING_KEY: ${{ secrets.WAVES_SIGNING_KEY }}
        run: |
          if [ -n "$WAVES_SIGNING_KEY" ]; then
            uv run --no-sync python tools/sign_manifest.py SHA256SUMS SHA256SUMS.sig
          else
            echo "::error::WAVES_SIGNING_KEY is not set; a tagged release MUST be signed or the in-app updater (fail-closed) will refuse every update. Add the WAVES_SIGNING_KEY secret before tagging."
            exit 1
          fi
      # The Release body comes from CHANGELOG.md: the section whose heading is
      # exactly "## <tag> (date)". Missing section = empty body + a warning,
      # never a failed release.
      - name: Extract release notes from CHANGELOG.md
        env:
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
        run: |
          awk -v tag="$RELEASE_TAG" '
            # Match the tag as any field on a "## " heading, so an optional emoji
            # accent (e.g. "## 🌊 v0.1.1 (date)") does not shift it off field 2.
            /^## / { on = 0; for (i = 2; i <= NF; i++) if ($i == tag) on = 1; next }
            on && (started || NF) { started = 1; print }
          ' CHANGELOG.md > RELEASE_NOTES.md
          # Every release body opens with the star-and-donate banner (kept in
          # step with the README's), so it cannot be forgotten again: it was
          # pasted in by hand for a while and quietly stopped after v0.1.5.
          if [ -s RELEASE_NOTES.md ]; then
            cat > NOTES_HEADER.md <<'BANNER'
          <p align="center">
            <strong>🌊 If you enjoy Waves, a star on this repo goes a long way, and if you'd like to help me afford to keep developing it, consider donating.</strong>
          </p>

          <p align="center">
            <a href="https://www.buymeacoffee.com/iamprivacy"><img src="https://img.shields.io/badge/-Buy%20me%20a%20coffee-ffdd00?style=flat&logo=buy-me-a-coffee&logoColor=black" alt="Buy Me a Coffee"></a>
          </p>

          BANNER
            cat NOTES_HEADER.md RELEASE_NOTES.md > RELEASE_NOTES.tmp && mv RELEASE_NOTES.tmp RELEASE_NOTES.md
          fi
          if [ -s RELEASE_NOTES.md ]; then
            echo "----- RELEASE_NOTES.md -----"; cat RELEASE_NOTES.md
          else
            echo "::warning::CHANGELOG.md has no '## ${RELEASE_TAG}' section; the Release body will be empty."
          fi
      - name: Set release notes body
        # The build job creates the Release (as a bodyless draft) before this job
        # runs, and softprops/action-gh-release does NOT overwrite the body of an
        # already-existing release, so its body_path below is silently ignored.
        # Set the notes explicitly here, while the release is still a draft.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
        run: |
          if [ -s RELEASE_NOTES.md ]; then
            gh release edit "$RELEASE_TAG" -R "$GITHUB_REPOSITORY" --notes-file RELEASE_NOTES.md
          fi
      - name: Publish manifest + signature
        uses: softprops/action-gh-release@e598afbe1493e6b1bafb1f389cabb956eab91231 # v3.0.3
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          fail_on_unmatched_files: false
          tag_name: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
          body_path: RELEASE_NOTES.md
          # This runs only after signing succeeded, so flipping draft→false here is
          # what makes the fully-signed Release visible to users. If signing failed
          # the job stops before this step and the Release stays a draft (invisible
          # to the updater) rather than going out unsigned.
          draft: false
          files: |
            SHA256SUMS
            SHA256SUMS.sig

  update-homebrew-tap:
    # A published release must reach every install channel, so once the signed
    # release is live this job bumps the Homebrew cask (Casks/waves.rb in the
    # tap repo) to the new version with the freshly built macOS checksums.
    # Without it, `brew upgrade` and Homebrew-managed in-app updates keep
    # serving the previous version until someone edits the tap by hand (which
    # is exactly how the cask once sat two releases stale).
    #
    # Auth: the TAP_PUSH_TOKEN secret, a fine-grained PAT with contents
    # read/write on the tap repo only (GITHUB_TOKEN cannot push to another
    # repo). If the secret is missing the job warns and exits green: the
    # release itself is already complete, the tap just needs a manual bump.
    needs: ["sign-manifest"]
    if: ${{ (github.event_name != 'workflow_dispatch' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag != '') }}
    runs-on: ubuntu-24.04
    env:
      TAP_REPO: iamprivacy/homebrew-waves
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
    steps:
      - name: Collect per-asset checksums
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: sha256-*
          merge-multiple: true
          path: sums
      - name: Bump the cask to this release
        env:
          GH_TOKEN: ${{ secrets.TAP_PUSH_TOKEN }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::warning::TAP_PUSH_TOKEN is not set; the Homebrew cask was NOT bumped. Update ${TAP_REPO} Casks/waves.rb to ${RELEASE_TAG} by hand (version + the macOS sha256s from SHA256SUMS, all four once the dual-flavor cask is live)."
            exit 0
          fi
          VERSION="${RELEASE_TAG#v}"
          # The cask covers the macOS zips; their checksum artifacts must
          # exist or the bump would write garbage, so fail loudly instead.
          ARM_SHA="$(awk '{print $1}' sums/waves_macos-apple-silicon.zip.sha256)"
          INTEL_SHA="$(awk '{print $1}' sums/waves_macos-intel.zip.sha256)"
          case "$ARM_SHA$INTEL_SHA" in
            *[!0-9a-f]*|"") echo "::error::macOS checksums missing or malformed; cask not bumped."; exit 1 ;;
          esac
          git clone --depth 1 "https://x-access-token:${GH_TOKEN}@github.com/${TAP_REPO}.git" tap
          cd tap
          if grep -q "on_sequoia" Casks/waves.rb; then
            # Dual-flavor cask: an on_sequoia block serves the regular bundles
            # and an on_sonoma :or_older block the legacy ones, so each sha256
            # pattern appears twice and every sed is scoped to its block.
            LEG_ARM_SHA="$(awk '{print $1}' ../sums/waves_macos-apple-silicon_legacy.zip.sha256)"
            LEG_INTEL_SHA="$(awk '{print $1}' ../sums/waves_macos-intel_legacy.zip.sha256)"
            case "$LEG_ARM_SHA$LEG_INTEL_SHA" in
              *[!0-9a-f]*|"") echo "::error::legacy macOS checksums missing or malformed; cask not bumped."; exit 1 ;;
            esac
            # Ranges anchor to the block OPENER at its exact indent: a comment
            # that merely mentions a block name must not start a range, or the
            # second sed re-covers the first block and both flavors end up
            # with the same checksums.
            sed -E -i "s/version \"[0-9][0-9.]*\"/version \"${VERSION}\"/" Casks/waves.rb
            sed -E -i "/^  on_sequoia /,/^  end$/ { s/arm:   \"[0-9a-f]{64}\"/arm:   \"${ARM_SHA}\"/; s/intel: \"[0-9a-f]{64}\"/intel: \"${INTEL_SHA}\"/; }" Casks/waves.rb
            sed -E -i "/^  on_sonoma /,/^  end$/ { s/arm:   \"[0-9a-f]{64}\"/arm:   \"${LEG_ARM_SHA}\"/; s/intel: \"[0-9a-f]{64}\"/intel: \"${LEG_INTEL_SHA}\"/; }" Casks/waves.rb
            grep -q "$LEG_ARM_SHA" Casks/waves.rb || { echo "::error::legacy arm sha256 not updated in cask"; exit 1; }
            grep -q "$LEG_INTEL_SHA" Casks/waves.rb || { echo "::error::legacy intel sha256 not updated in cask"; exit 1; }
          else
            sed -E -i \
              -e "s/version \"[0-9][0-9.]*\"/version \"${VERSION}\"/" \
              -e "s/arm:   \"[0-9a-f]{64}\"/arm:   \"${ARM_SHA}\"/" \
              -e "s/intel: \"[0-9a-f]{64}\"/intel: \"${INTEL_SHA}\"/" \
              Casks/waves.rb
          fi
          # Every value must have landed; a silent partial edit would ship a
          # cask whose checksum can never match the download.
          grep -q "version \"${VERSION}\"" Casks/waves.rb || { echo "::error::version not updated in cask"; exit 1; }
          grep -q "$ARM_SHA" Casks/waves.rb || { echo "::error::arm sha256 not updated in cask"; exit 1; }
          grep -q "$INTEL_SHA" Casks/waves.rb || { echo "::error::intel sha256 not updated in cask"; exit 1; }
          if git diff --quiet; then
            echo "Cask already at ${VERSION}; nothing to push."
            exit 0
          fi
          git config user.name "waves-release"
          git config user.email "waves-release@users.noreply.github.com"
          git add Casks/waves.rb
          git commit -m "waves ${VERSION}"
          git push
          echo "Cask bumped to ${VERSION}."
