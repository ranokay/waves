# Review-thread dispositions (R-02)

Source: audit finding HD-04 (thread capture in the untracked audit
workspace `docs/audits/full-codebase-2026-09-16/evidence/history/unresolved-threads.json`,
not committed to the repo) and issue #226. Each row's discussion URL is the
stable primary key — the scope re-derives from GitHub alone — and the guard
test embeds all 98 ids. At capture time 107
review threads stood unresolved on 9 merged PRs: 33 P1, 65 P2, 1 P3 and
8 unbadged. This table dispositions the 98 P1/P2 threads; the P3 and the
unbadged threads are outside R-02's scope.

Method: every thread below was re-checked live on 2026-09-21 — all 98
still stand unresolved with no replies, so each disposition comes from
the current code, not from thread discussion. Citations use the
post-#344 package layout (`waves/desktop/`, `waves/library/`,
`waves/metadata/`, `waves/providers/`) as read on develop `8759829`
(2026-09-21); line numbers will drift, the discussion URL plus the
quoted construct re-locates each one. Every `fixed-in` SHA is a
develop squash (or, for PR #37's true-merge, the branch commit) verified
present on `develop`; the cited lines were read, not inferred. Ten
threads describe defects that are still live; each is deferred to a
tracked follow-up issue rather than fixed here, so this change stays a
record. Verdicts: `fixed-in <sha>`, `refuted` (the claim is wrong
against the code, with the disproof), `deferred → #n`.

Counts: 76 fixed-in, 12 refuted, 10 deferred → #360–#367, 0 wont-fix.
Sections lead with PR #57's Atmos group and PR #48's quarantine/retry
groups, as the issue asks, then the rest in PR order.

## PR #57 — Atmos fragments, twins and badges (21, all P2, all fixed-in `a90d655`)

| Thread                                                          | Severity | Finding                                 | Disposition        | Evidence                                                                                                                             |
| --------------------------------------------------------------- | -------- | --------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| https://github.com/ranokay/waves/pull/57#discussion_r3966143683 | P2       | Resolve full nested Atmos fragment      | fixed-in `a90d655` | `waves/desktop/bridge_library.py:588-594` — `_atmos_parent` matches the complete fragment past non-audio intermediates.              |
| https://github.com/ranokay/waves/pull/57#discussion_r3966143687 | P2       | Group rows before folding Atmos         | fixed-in `a90d655` | `waves/desktop/bridge_library.py:946-949` — one `by_folder` map built once, reused by both folds.                                    |
| https://github.com/ranokay/waves/pull/57#discussion_r3966143692 | P2       | Twin key needs track artist             | fixed-in `a90d655` | `waves/library/index.py:2392-2407` + `waves/metadata/matching.py:1075-1085` — partition uses the (title, artist) `twin_key`.         |
| https://github.com/ranokay/waves/pull/57#discussion_r3966463155 | P2       | Coalesce repeated Atmos-only Versions   | fixed-in `a90d655` | `waves/library/index.py:2422-2430` — `promoted_lengths` attaches same-seconds repeats; bridge mirror `:676-683`.                     |
| https://github.com/ranokay/waves/pull/57#discussion_r3966463161 | P2       | Persist unknown type for retry          | fixed-in `a90d655` | `waves/library/index.py:2280-2282` — unknown persisted, never laundered to stereo, freshness gate retries.                           |
| https://github.com/ranokay/waves/pull/57#discussion_r3966569063 | P2       | Album identity from canonical file      | fixed-in `a90d655` | `waves/library/index.py:2443-2458` — first proven-stereo (else unknown/promoted) speaks for the release.                             |
| https://github.com/ranokay/waves/pull/57#discussion_r3966569071 | P2       | Keep distinct untitled Atmos tracks     | fixed-in `a90d655` | `waves/library/index.py:2396-2398` — only titled keys dedupe; untitled entries stay separate.                                        |
| https://github.com/ranokay/waves/pull/57#discussion_r3966569078 | P2       | Fold promoted durations into runtime    | fixed-in `a90d655` | `waves/desktop/bridge_library.py:656-658` + `:965-971` — `extra_runtime` joins the runtime or silences the witness.                  |
| https://github.com/ranokay/waves/pull/57#discussion_r3966569087 | P2       | Validate every fragment component       | fixed-in `a90d655` | `waves/desktop/bridge_library.py:535-570` — `_match_parses` aligns every level back-to-front.                                        |
| https://github.com/ranokay/waves/pull/57#discussion_r3966697718 | P2       | Promoted first file votes               | fixed-in `a90d655` | `waves/library/index.py:2471-2493` — vote runs on the canonical set; only attached Versions sit out.                                 |
| https://github.com/ranokay/waves/pull/57#discussion_r3966697727 | P2       | Distinct same-named Atmos tracks        | fixed-in `a90d655` | `waves/library/index.py:2399-2405` + `:2422-2429` — dedupe needs title+artist+seconds; lengthless pairs both count.                  |
| https://github.com/ranokay/waves/pull/57#discussion_r3966765847 | P2       | Length evidence for stereo attach       | fixed-in `a90d655` | `waves/library/index.py:2411-2421` + `waves/metadata/matching.py:1095-1103` — `meets_twin` checks seconds against stereo candidates. |
| https://github.com/ranokay/waves/pull/57#discussion_r3966836787 | P2       | Anchor placeholder literal positions    | fixed-in `a90d655` | `waves/desktop/bridge_library.py:493-532` — leading/trailing literals anchor; Atmosphere/Deluxe rejected.                            |
| https://github.com/ranokay/waves/pull/57#discussion_r3966928090 | P2       | Normalize fragments like destinations   | fixed-in `a90d655` | `waves/desktop/bridge_library.py:459-478` — fragments unioned with pipeline-sanitized spellings.                                     |
| https://github.com/ranokay/waves/pull/57#discussion_r3967008092 | P2       | Sanitize at true component boundaries   | fixed-in `a90d655` | `waves/desktop/bridge_library.py:420-456` — literal-only components sanitize whole; token edges preserved.                           |
| https://github.com/ranokay/waves/pull/57#discussion_r3967099651 | P2       | Rebuild index on fragment change        | fixed-in `a90d655` | `waves/desktop/backend.py:21454-21470` — a changed `format_atmos` sets lib_edit, warm rebuild follows.                               |
| https://github.com/ranokay/waves/pull/57#discussion_r3967099658 | P2       | Empty-render placeholder components     | fixed-in `a90d655` | `waves/desktop/bridge_library.py:540-545` + `:563-565` — a placeholder level consumes one level or none.                             |
| https://github.com/ranokay/waves/pull/57#discussion_r3967253122 | P2       | Sanitize rendered placeholder fragments | fixed-in `a90d655` | `waves/desktop/bridge_library.py:435-456` — chunks beside tokens sanitize with edges kept.                                           |
| https://github.com/ranokay/waves/pull/57#discussion_r3967253139 | P2       | All-Atmos fold is not ATMOS TOO         | fixed-in `a90d655` | `waves/desktop/bridge_library.py:651-655` + `:661` — the flag derives from parent stereo, not the fold.                              |
| https://github.com/ranokay/waves/pull/57#discussion_r3967390200 | P2       | Interior dots survive in fragments      | fixed-in `a90d655` | `waves/desktop/bridge_library.py:515-532` — dot trim only at a component's true end.                                                 |
| https://github.com/ranokay/waves/pull/57#discussion_r3967390207 | P2       | TOO needs proven stereo                 | fixed-in `a90d655` | `waves/library/index.py:2442` — `has_atmos` requires an explicit stereo row; bridge `:635`, `:661`.                                  |

## PR #48 — quarantine, retry, decode errors (30: 27 fixed-in `2e8c564`, 3 deferred)

| Thread                                                          | Severity | Finding                                                 | Disposition        | Evidence                                                                                                                                                                                     |
| --------------------------------------------------------------- | -------- | ------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| https://github.com/ranokay/waves/pull/48#discussion_r3961848059 | P1       | Route resolve-stage failures into retry loop            | fixed-in `2e8c564` | `waves/providers/apple/runner.py:1738` — resolve-stage raises still enter the integrity retry/quarantine path below.                                                                         |
| https://github.com/ranokay/waves/pull/48#discussion_r3961848065 | P1       | Keep retry bypass active for whole collection           | fixed-in `2e8c564` | `waves/desktop/backend.py:3174` — `is_retry` is a per-row spec flag, never shared or discarded mid-collection.                                                                               |
| https://github.com/ranokay/waves/pull/48#discussion_r3961848076 | P2       | Restrict Quarantine exclusion to configured path        | fixed-in `2e8c564` | `waves/library/index.py:133` — custom locations judged by full path, never basename.                                                                                                         |
| https://github.com/ranokay/waves/pull/48#discussion_r3961848086 | P2       | Remove superseded retry hold directories                | fixed-in `2e8c564` | `waves/providers/apple/runner.py:1881` — a previous hold's temp dir is removed when a newer hold replaces it.                                                                                |
| https://github.com/ranokay/waves/pull/48#discussion_r3961959298 | P2       | Reject plain AC-3 for Atmos requests                    | fixed-in `2e8c564` | `waves/providers/apple/engine.py:237` — the Atmos allowlist is eac3/ec3/ac4 only, no plain ac3.                                                                                              |
| https://github.com/ranokay/waves/pull/48#discussion_r3962050519 | P1       | Preserve engine-rejected bytes for Quarantine           | fixed-in `2e8c564` | `waves/providers/apple/runner.py:1815` — resolve-stage failure carries staged_path/workdir outward for the date read and the hold.                                                           |
| https://github.com/ranokay/waves/pull/48#discussion_r3962050528 | P1       | Treat no-audio probe failures as integrity              | fixed-in `2e8c564` | `waves/providers/apple/runner.py:804` — "no playable audio stream" is an integrity verdict with retry/quarantine.                                                                            |
| https://github.com/ranokay/waves/pull/48#discussion_r3962050537 | P2       | Register persisted Quarantine paths before scanning     | fixed-in `2e8c564` | `waves/desktop/backend.py:20654` — startup/settings configure registers remembered roots before any scan.                                                                                    |
| https://github.com/ranokay/waves/pull/48#discussion_r3962050542 | P2       | Delete retry hold when cancellation interrupts pacing   | fixed-in `2e8c564` | `waves/providers/apple/runner.py:1809` — the abort path drops the hold dir before re-raising.                                                                                                |
| https://github.com/ranokay/waves/pull/48#discussion_r3962180503 | P1       | Reject decode errors despite zero exit                  | fixed-in `2e8c564` | `waves/providers/apple/engine.py:190` — any ffmpeg stderr fails the file, not just a nonzero exit.                                                                                           |
| https://github.com/ranokay/waves/pull/48#discussion_r3962180510 | P1       | Key retry bypasses by Version job                       | fixed-in `2e8c564` | `waves/desktop/backend.py:3171` — dual-Version rows carry their own flag, no shared set entry.                                                                                               |
| https://github.com/ranokay/waves/pull/48#discussion_r3962180513 | P2       | Exclude cached Quarantine roots from walk frontier      | fixed-in `2e8c564` | `waves/library/index.py:1798` — cached submissions check the quarantine path and condemn the subtree instead of descending.                                                                  |
| https://github.com/ranokay/waves/pull/48#discussion_r3962287135 | P1       | Release each retry-bypass permit only once              | fixed-in `2e8c564` | `waves/providers/apple/runner.py:2259` — nothing counted or released; the bypass rides the row spec.                                                                                         |
| https://github.com/ranokay/waves/pull/48#discussion_r3962407388 | P1       | Reject download root as custom Quarantine path          | fixed-in `2e8c564` | `waves/providers/apple/integrity.py:120` — a base within the candidate falls back to the default Waves Quarantine.                                                                           |
| https://github.com/ranokay/waves/pull/48#discussion_r3962407397 | P2       | Release retry permits when dispatch fails early         | fixed-in `2e8c564` | `waves/providers/apple/runner.py:2260` — the spec flag dies with its row, so undispatched jobs leak nothing.                                                                                 |
| https://github.com/ranokay/waves/pull/48#discussion_r3962539240 | P1       | Preserve retry state when gating a replay               | fixed-in `2e8c564` | `waves/desktop/backend.py:13871` — deferred ffmpeg/folder replays carry the row's keep_ask and is_retry.                                                                                     |
| https://github.com/ranokay/waves/pull/48#discussion_r3962539249 | P1       | Reject quarantine roots containing download library     | fixed-in `2e8c564` | `waves/providers/apple/integrity.py:120` — ancestor overlap via `_is_within` falls back to the default.                                                                                      |
| https://github.com/ranokay/waves/pull/48#discussion_r3962539254 | P2       | Respect case-sensitive directory identities             | fixed-in `2e8c564` | `waves/providers/apple/integrity.py:62` — casefold gated to Windows/macOS; Linux compares exactly.                                                                                           |
| https://github.com/ranokay/waves/pull/48#discussion_r3962592789 | P2       | Ignore release dates classifying outbreak files         | fixed-in `2e8c564` | `waves/providers/apple/integrity.py:351` — ©-day deliberately excluded; only genuine Encoded/creation tags count.                                                                           |
| https://github.com/ranokay/waves/pull/48#discussion_r3962592794 | P2       | Resolve aliases checking ancestor quarantine paths      | fixed-in `2e8c564` | `waves/providers/apple/integrity.py:96` — realpath-canonicalized spelling plus samefile settles symlinked ancestors.                                                                         |
| https://github.com/ranokay/waves/pull/48#discussion_r3962592801 | P2       | Normalize quarantine paths per filesystem case rules    | fixed-in `2e8c564` | `waves/library/index.py:209` — a case-only mismatch is confirmed via samefile, not spelling alone.                                                                                           |
| https://github.com/ranokay/waves/pull/48#discussion_r3962592806 | P2       | Remove held bytes when later retry fails normally       | fixed-in `2e8c564` | `waves/providers/apple/runner.py:1845` — a non-integrity exit drops the earlier hold before re-raising.                                                                                      |
| https://github.com/ranokay/waves/pull/48#discussion_r3962661472 | P2       | Key fallback deliveries by delivered Version            | fixed-in `2e8c564` | `waves/providers/apple/runner.py:1901` — persistent failure files land under delivered atmos/stereo, not the ask hint.                                                                       |
| https://github.com/ranokay/waves/pull/48#discussion_r3962661478 | P2       | Keep previous Quarantine roots excluded across restarts | fixed-in `2e8c564` | `waves/desktop/backend.py:20654` — every remembered sidecar root is registered at configure.                                                                                                 |
| https://github.com/ranokay/waves/pull/48#discussion_r3962785723 | P2       | Key resolve-stage failures by effective Version         | fixed-in `2e8c564` | `waves/providers/apple/runner.py:1904` — the info-None branch reads effective_version (asked AND offered) first.                                                                             |
| https://github.com/ranokay/waves/pull/48#discussion_r3962785729 | P2       | Retain every previous custom Quarantine root            | fixed-in `2e8c564` | `waves/providers/apple/integrity.py:166` — the remember list is uncapped by design; no oldest-root eviction.                                                                                 |
| https://github.com/ranokay/waves/pull/48#discussion_r3962877823 | P1       | Reject Quarantine ancestors of the separate library     | deferred → #360    | `waves/providers/apple/integrity.py:103` — the resolver takes only `download_base`, while `waves/desktop/bridge_library.py:752` proves the scan root can be an independent `library_folder`. |
| https://github.com/ranokay/waves/pull/48#discussion_r3962877833 | P2       | Delete the Quarantine sidecar during factory reset      | deferred → #361    | `waves/providers/apple/integrity.py:134` names `apple_quarantine_roots.json` (+`.tmp` at `:171`); `waves/desktop/backend.py:647` `_FACTORY_WIPE_FILES` lists neither.                        |
| https://github.com/ranokay/waves/pull/48#discussion_r3962877839 | P2       | Remove stale Quarantine bytes after recovery            | deferred → #362    | `waves/providers/apple/runner.py:2448` clears only the SQLite mark (via `waves/library/ownership.py:866`); the disk artifact is never removed and no path is recorded.                       |
| https://github.com/ranokay/waves/pull/48#discussion_r3962877844 | P2       | Canonicalize walked paths before Quarantine matching    | fixed-in `2e8c564` | `waves/library/index.py:2097` — fresh listings check the full walked path via the canonicalizing `_is_quarantine_path`.                                                                      |

## PR #37 — seam resolver binding, artist credits (2: fixed in `5e01ab0` on PR #37's branch; true-merge `0499364`)

| Thread                                                          | Severity | Finding                                    | Disposition        | Evidence                                                                                                                                                                |
| --------------------------------------------------------------- | -------- | ------------------------------------------ | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| https://github.com/ranokay/waves/pull/37#discussion_r3910736930 | P1       | Keep resolver bound to active download     | fixed-in `5e01ab0` | `waves/providers/tidal.py:193` — `stream_resolver_bound` scopes each bind per resolve and restores the previous, so a settings-save rebuild cannot steal a running job. |
| https://github.com/ranokay/waves/pull/37#discussion_r3910736935 | P2       | Keep credited names when artist IDs absent | fixed-in `5e01ab0` | `waves/providers/tidal.py:776` — the artists list keeps every name with an empty-identity fallback; IDs filtered separately at `:769`.                                  |

## PR #44 — Apple catalog pages (3)

| Thread                                                          | Severity | Finding                                         | Disposition        | Evidence                                                                                                                                                           |
| --------------------------------------------------------------- | -------- | ----------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| https://github.com/ranokay/waves/pull/44#discussion_r3953672477 | P1       | Refetch summaries before building catalog pages | fixed-in `27955bd` | `waves/providers/apple/provider.py:676` — `get_object` serves cache only if in `_complete` or `_is_complete`, else refetches via `_fetch_*`.                       |
| https://github.com/ranokay/waves/pull/44#discussion_r3953672481 | P2       | Follow pagination for collection relationships  | fixed-in `08e162c` | `waves/providers/apple/provider.py:784` — `_exhaust_response`/`_exhaust_pages` appends every continuation, raising `AppleCollectionIncomplete` on partial failure. |
| https://github.com/ranokay/waves/pull/44#discussion_r3953672484 | P2       | Route Apple hover prefetches through Apple      | fixed-in `27955bd` | `waves/desktop/backend.py:6869` — `prefetchArtist`'s apple: branch builds the Apple page silently via `_start_apple_artist_build`.                                 |

## PR #45 — artist cache and fetched-object completeness (5, all fixed-in `27955bd`)

| Thread                                                          | Severity | Finding                                               | Disposition        | Evidence                                                                                                                                       |
| --------------------------------------------------------------- | -------- | ----------------------------------------------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| https://github.com/ranokay/waves/pull/45#discussion_r3953808040 | P2       | Publish warmed cache before clearing in-flight mark   | fixed-in `27955bd` | `waves/desktop/backend.py:6750` — payload published inside try before finally releases `_artist_loading` at `:6767`.                           |
| https://github.com/ranokay/waves/pull/45#discussion_r3953842699 | P1       | Summaries must invalidate fetched-object completeness | fixed-in `27955bd` | `waves/providers/apple/provider.py:510` — `_remember` discards (kind, raw_id) from `_complete` when a different object is stored.              |
| https://github.com/ranokay/waves/pull/45#discussion_r3953874168 | P2       | Recheck warmed cache while holding prefetch lock      | fixed-in `27955bd` | `waves/desktop/backend.py:6698` — click rechecks `_artist_cache` under `_prefetch_lock`; hover twin at `:6879`.                                |
| https://github.com/ranokay/waves/pull/45#discussion_r3953874171 | P2       | Stale workers must not clear new prefetch             | fixed-in `27955bd` | `waves/desktop/backend.py:6766` — teardown guarded by gen == `_browse_gen`; a stale generation clears nothing.                                 |
| https://github.com/ranokay/waves/pull/45#discussion_r3953917560 | P2       | Rendering fetched object must preserve completeness   | fixed-in `27955bd` | `waves/providers/apple/provider.py:510` — the `is not item` identity check keeps the marker when `row_for` renders the identical fetched dict. |

## PR #46 — Apple downloads: provisioning, delivery, job gates (18: 14 fixed, 4 deferred)

| Thread                                                          | Severity | Finding                                     | Disposition        | Evidence                                                                                                                                                                                                                                                |
| --------------------------------------------------------------- | -------- | ------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| https://github.com/ranokay/waves/pull/46#discussion_r3954270556 | P1       | Read Apple ownership from bridge store      | fixed-in `e4f051b` | `waves/desktop/backend.py:1815` (`self._ownership_of` injected callable) + `waves/providers/apple/runner.py:1280` (store `ownership_of` with audio_type); no `_ownership_of` lookup on WavesBridge.                                                     |
| https://github.com/ranokay/waves/pull/46#discussion_r3954270560 | P1       | Provision N_m3u8DL-RE before downloads      | fixed-in `c400fb9` | `waves/desktop/backend.py:13846-13856` binary gate routes to the wizard runtime step + `waves/providers/apple/runtime.py:761-815` installs/verifies/checksums the pinned release fail-closed.                                                           |
| https://github.com/ranokay/waves/pull/46#discussion_r3954270564 | P1       | Propagate managed FFmpeg path to Apple      | fixed-in `e4f051b` | `waves/desktop/backend.py:20613-20621` — `_configure_apple_provider` calls `_resolve_ffmpeg()` then sets the managed injection, plus the nm3u8dlre resolver and wrapper URL.                                                                            |
| https://github.com/ranokay/waves/pull/46#discussion_r3954270567 | P2       | Check destination before collision name     | fixed-in `e4f051b` | `waves/providers/apple/runner.py:1688-1708` — asking `pick_destination` first would make the exists check permanently false; exact=base/relative checked first, `pick_destination` only in the else branch.                                             |
| https://github.com/ranokay/waves/pull/46#discussion_r3954340351 | P1       | Decode staged audio before accepting        | fixed-in `e4f051b` | `waves/providers/apple/runner.py:703-748` `verify_staged` asserts codec family + ffmpeg decode-to-null; `waves/providers/apple/engine.py:150-162` ffprobe checks the sibling beside managed ffmpeg first, `:165-193` decode check fails on stderr/exit. |
| https://github.com/ranokay/waves/pull/46#discussion_r3954340359 | P1       | Make delivery replacement atomic            | fixed-in `e4f051b` | `waves/providers/apple/runner.py:666-683` `place_file` copies beside the target to `.part-<rand>` then `os.replace`; tmp unlinked on failure; forced overwrites never delete the good copy first.                                                       |
| https://github.com/ranokay/waves/pull/46#discussion_r3954340364 | P1       | Preserve owned collision path on force      | fixed-in `e4f051b` | `waves/providers/apple/runner.py:1698-1703` — force keeps the owned path, else the exact path; a template path would overwrite the sibling's audio while its record goes stale.                                                                         |
| https://github.com/ranokay/waves/pull/46#discussion_r3954340368 | P1       | Retry throttled deliveries in place         | fixed-in `d490cbe` | `waves/providers/apple/runner.py:2368-2393` classifies THROTTLED then `throttle_delay` (`:1371-1380` honors Retry-After) + `throttle_wait` countdown, retrying the same track abortably.                                                                |
| https://github.com/ranokay/waves/pull/46#discussion_r3954380373 | P1       | Honor cancellation after reachability probe | deferred → #363    | `waves/providers/apple/runner.py:2537-2543` returns on gate-false without a `job_abort` check or replay discard, while `waves/desktop/backend.py:15025` (TIDAL) discards pending downloads and releases the hold.                                       |
| https://github.com/ranokay/waves/pull/46#discussion_r3954380378 | P2       | Wait on the pause gate between Apple tracks | deferred → #363    | `waves/providers/apple/runner.py:2209-2211` checks only `job_abort`; the engine (`waves/download.py:1106-1127`) waits on `_event_run` when paused.                                                                                                      |
| https://github.com/ranokay/waves/pull/46#discussion_r3954380383 | P2       | Use cached album artist for tags            | deferred → #364    | `waves/providers/apple/provider.py:1506-1523` loads `album_attrs` but builds `album_artists` from the track's `artistName`.                                                                                                                             |
| https://github.com/ranokay/waves/pull/46#discussion_r3954380387 | P2       | Respect metadata URL preference             | deferred → #365    | `waves/providers/apple/files.py:316` passes `url_share` unconditionally; the engine gates it (`waves/download.py:4187-4189`).                                                                                                                           |
| https://github.com/ranokay/waves/pull/46#discussion_r3954431703 | P1       | Keep sanitized paths relative               | fixed-in `e4f051b` | `waves/paths.py:518-548` `_drop_empty_segments` drops empties and names `.`/`..` via DOT_SEGMENT_STANDIN; `waves/providers/apple/files.py:130-150` calls it and `pick_destination` rejects empty/absolute.                                              |
| https://github.com/ranokay/waves/pull/46#discussion_r3954431707 | P2       | Create playlist files for collections       | fixed-in `84e3e20` | `waves/providers/apple/runner.py:2460-2475` writes `_Name.m3u8` via `write_collection_playlist` when playlist_create and landed; `waves/playlists.py:245-273` shared per-directory writer used by both providers.                                       |
| https://github.com/ranokay/waves/pull/46#discussion_r3954431709 | P2       | Gate standalone cover on option             | fixed-in `e4f051b` | `waves/providers/apple/runner.py:264-268` `_want_cover_file` requires save_cover and (collection or single_track); enforced at `:1126-1136`, `:1964-1976`, `:1238-1242`.                                                                                |
| https://github.com/ranokay/waves/pull/46#discussion_r3954489964 | P2       | Check identity before skipping collision    | fixed-in `c612827` | `waves/providers/apple/runner.py:1705` exact.exists + `occupant_is_version(exact, version_hint)` else a numbered variant; true-ext re-gate `:1777-1784`. The R-12 version-aware skip.                                                                   |
| https://github.com/ranokay/waves/pull/46#discussion_r3954489970 | P2       | Preserve skipped tracks in playlists        | fixed-in `84e3e20` | `waves/playlists.py:246-287` `_playlist_entries` only reorders folder contents, returning the folder listing when counts disagree — a partial landed list never shrinks a complete playlist.                                                            |
| https://github.com/ranokay/waves/pull/46#discussion_r3954489973 | P2       | Honor separate cover-file dimension         | fixed-in `90ba532` | `waves/providers/apple/runner.py:1185-1205` `cover_bytes_pair` reads the dimension via `constants.cover_file_dimension`; deliver `:1967-1976` and standalone COVER use it.                                                                              |

## PR #47 — dual-version rows (4: 2 fixed, 2 deferred)

| Thread                                                          | Severity | Finding                                         | Disposition        | Evidence                                                                                                                                                            |
| --------------------------------------------------------------- | -------- | ----------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| https://github.com/ranokay/waves/pull/47#discussion_r3954821773 | P1       | Atmos rows must hold pin when setting changes   | fixed-in `eef6a23` | `waves/download.py:2768` — want_atmos decided from the pinned audio_type arg, never live Settings; shared rule at `waves/constants.py:211`. The R-13 per-click ask. |
| https://github.com/ranokay/waves/pull/47#discussion_r3954821776 | P1       | Blank Atmos folder must not swallow one version | fixed-in `c612827` | `waves/providers/apple/runner.py:1705` — skip raises `_AppleSkipped` only on version match; TIDAL twin at `waves/download.py:2046`. The R-12 version-aware skip.    |
| https://github.com/ranokay/waves/pull/47#discussion_r3954821780 | P1       | Bulk rollups must wait for both dual rows       | deferred → #366    | `waves/desktop/backend.py:15456` — groups credit `media_id` done on the first row's terminal state while the sibling Version still runs.                            |
| https://github.com/ranokay/waves/pull/47#discussion_r3954821782 | P1       | Redownload force must cover every dual row      | deferred → #366    | `waves/desktop/backend.py:15193` discards `_redownload_overrides` on first success while `:14920` reads it per row at dispatch.                                     |

## PR #51 — runtime, wizard, container readiness (14: 2 fixed, 12 refuted)

| Thread                                                          | Severity | Finding                                       | Disposition        | Evidence                                                                                                                                                                                                                                                  |
| --------------------------------------------------------------- | -------- | --------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| https://github.com/ranokay/waves/pull/51#discussion_r3963933125 | P1       | Reject APKs until checksum pinned             | fixed-in `cfe6a0f` | `waves/providers/apple/runtime.py:7-8` ("no APK is provisioned or supplied at runtime") + `waves/desktop/qml/SettingsPage.qml:3971-3974` (no APK extraction block by design); the user-APK verify/plan path is deleted, the image carries the guest libs. |
| https://github.com/ranokay/waves/pull/51#discussion_r3963933132 | P1       | Render mandatory full-tier setup instructions | fixed-in `cfe6a0f` | `waves/desktop/qml/SettingsPage.qml:3976-3980` renders the wrapper login hint; `waves/desktop/backend.py:19348-19361` says no APK to supply; the extract plan died with the user-APK path.                                                                |
| https://github.com/ranokay/waves/pull/51#discussion_r3963933136 | P1       | Move container detection off GUI thread       | refuted            | `waves/desktop/backend.py:20792-20810` — `_apple_container_state` never runs a subprocess on the GUI thread: a fresh cache wins, stale served while a worker refreshes.                                                                                   |
| https://github.com/ranokay/waves/pull/51#discussion_r3963933139 | P2       | Display runtime failures and progress         | refuted            | `waves/desktop/qml/SettingsPage.qml:3962-3966` shows state and pct text; `waves/desktop/backend.py:19472/19481/19691` emits failed/done plus a status refresh. Present since the PR itself.                                                               |
| https://github.com/ranokay/waves/pull/51#discussion_r3963933144 | P2       | Continue probing after stopped Docker         | refuted            | `waves/providers/apple/runtime.py:216-217` — "a stopped Docker must not hide a running Podman"; the loop continues. Present since the PR itself.                                                                                                          |
| https://github.com/ranokay/waves/pull/51#discussion_r3964074448 | P1       | Keep cookies step open until downloads start  | refuted            | `waves/desktop/backend.py:21151-21160` — signed_in requires `verify_cookies_file` AND `_apple_fetch_binary_ready()`; downloads without a binary route to the runtime step.                                                                                |
| https://github.com/ranokay/waves/pull/51#discussion_r3964074451 | P1       | Do not label removal as Continue              | refuted            | `waves/desktop/backend.py:1219-1228` maps apple_remove_runtime to "Remove"; `waves/desktop/qml/SettingsPage.qml:3843` renders the action label else CONTINUE. Present since the PR itself.                                                                |
| https://github.com/ranokay/waves/pull/51#discussion_r3964074452 | P1       | Move stale cache refresh off GUI              | refuted            | `waves/desktop/backend.py:20808-20810` — stale cache returned immediately with a worker refresh scheduled underneath; the inline refresh runs only with no worker pool (test stubs). No 6 s GUI block.                                                    |
| https://github.com/ranokay/waves/pull/51#discussion_r3964074456 | P2       | Start detected Podman, not Docker             | refuted            | `waves/providers/apple/runtime.py:238-268` — gentle start is runtime-specific and returns the real exit code; `waves/desktop/backend.py:19608-19611` passes the cached name. Present since the PR itself.                                                 |
| https://github.com/ranokay/waves/pull/51#discussion_r3964074458 | P2       | Count configured binaries as ready            | refuted            | `waves/desktop/backend.py:20724-20760` — readiness checks the override, the managed copy, then PATH; `waves/providers/apple/runtime.py` manager status reports path state. Not managed-only.                                                              |
| https://github.com/ranokay/waves/pull/51#discussion_r3964219497 | P2       | Hide start where no command exists            | refuted            | `waves/desktop/backend.py:19324-19330` — startable is None without a gentle-start command and the action is empty; non-Darwin keeps manual guidance, no dead button.                                                                                      |
| https://github.com/ranokay/waves/pull/51#discussion_r3964219500 | P2       | Validate overrides before reporting ready     | refuted            | `waves/desktop/backend.py:20750-20753` — a candidate counts only if it is an executable file; "a picked text file must not light the tier green". The "any regular file" claim is false.                                                                  |
| https://github.com/ranokay/waves/pull/51#discussion_r3964219502 | P2       | Coalesce concurrent cache refreshes           | refuted            | `waves/desktop/backend.py:20821-20843` — the refresh lock is acquired non-blocking; an in-flight call is a no-op success. Present since the PR itself.                                                                                                    |
| https://github.com/ranokay/waves/pull/51#discussion_r3964219504 | P2       | Let wrapper port override win                 | refuted            | `waves/desktop/backend.py:19186-19190` + `:1425-1455` — "explicit override wins over persisted pick"; `_apple_effective_port` prefers the free unprivileged override. Present since the PR itself.                                                        |

## PR #56 — Chooser deferred confirmation (1, deferred)

| Thread                                                          | Severity | Finding                                        | Disposition     | Evidence                                                                                                                                                                     |
| --------------------------------------------------------------- | -------- | ---------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| https://github.com/ranokay/waves/pull/56#discussion_r3965893583 | P2       | Emit Chooser confirmation after deferred gates | deferred → #367 | `waves/desktop/backend.py:10273` confirms only on the direct path; gate-held replays (`:13505`, `:13862`) and `_run_pending_downloads` (`:13264`) discard the replay result. |
